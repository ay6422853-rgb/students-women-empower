# Frontend

Run:

npm install
npm run dev

The starter includes login, registration, a responsive role-aware dashboard shell, and API integration.
Expand the placeholder sidebar links into full pages as the backend modules are completed.
